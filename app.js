const eventData = {
  "technical": [
    {
      "id": "debugging",
      "name": "Debugging",
      "description": "Think you’ve got what it takes to outsmart the bugs? Step into the coder’s arena where every error is a challenge and every fix is a victory! Join us on September 4th, 2025, from 2 PM to 3 PM for a high-pressure debugging battle where you’ll face tricky, misleading, and sneaky code errors in C, Java, or Python. Only the sharpest minds and fastest fingers will survive each round. Show off your programming skills, logical thinking, and speed to claim the title of Ultimate Debugger!",
      "guidelines": [
        "Debugging will be conducted in C, Java, or Python, Team size: 1 Member",
        "Round 1: Offline (20 Minutes) (should bring their own pen)",
        "A Question Paper with Set of 6 questions will be Given , the Participants should find the error using comments",
        "Based on Finding the number of Errors and Fastest submission – Top 5 will qualify to Round 2",
        "Round 2:"
        "Each Person will be provided a error code of 4 , the participant should submit their code after correcting , for correct output 10 points will be added , for partial output 5 points and for wrong one -2"
      ],
      "registerLink": "https://forms.gle/rqgEuu5pMPcjwhvB8"
    },
    {
      "id": "ideathon",
      "name": "Ideathon",
      "description": "Showcase your research, ideas, and technical insights in the Paper Presentation event. This is your platform to highlight problem-solving skills, propose innovative solutions, and demonstrate your expertise through a structured presentation. The event will be held on Sept 4th, 2025, from 11:00 AM - 1:00 PM.",
      "guidelines": [
        "Team size: 1 - 3 members",
        "Participants must submit their PPT by Sept 3rd, 2025",
        "Maximum slide count: 10 - 12 slides",
        "Presentation duration: 7 mins + 2 mins Q&A",
        "PPT must clearly cover: Problem Statement, Proposed Solution, Technology Stack"
      ],
      "registerLink": "https://forms.gle/NcpLHMm62ihpcgMh9"
    },
    {
      "id": "logo-design",
      "name": "Logo Design Contest",
      "description": "Like a hero’s grand entry in a blockbuster movie, this is your moment! Your creativity is the hero, your tools are the weapons, and the theme is the twist you’ll discover on the spot. Design a logo that speaks louder than words and leaves the judges speechless. The contest will be held on Sept 4th, 11:30 AM - 1:00 PM.",
      "guidelines": [
        "Team size: 1 participant",
        "Theme will be revealed at the start of the event",
        "Allowed tools: Canva, Adobe Illustrator, Photoshop, Looka, or similar",
        "AI restriction: No AI tools (ChatGPT, Gemini, etc.) - violation = direct disqualification",
        "Judging criteria: Creativity, relevance to theme, originality, and presentation"
      ],
      "registerLink": "https://forms.gle/ocG9QE98wad64NKfA"
    },
    {
      "id": "web-design-ai",
      "name": "AI Web Design Challenge",
      "description": "Turn AI into your co-pilot and race against the clock in this exciting design challenge on Sept 3rd, 2:00 PM - 3:30 PM. With one hour, one theme, and unlimited creativity, craft a stunning website that tells a story. Remember - design is the body, explanation is the soul.",
      "guidelines": [
        "Team Size: 1 - 2 members",
        "Theme: Revealed at the event start",
        "Time Limit: 1 hr 10 mins with system & internet provided",
        "AI Use: Allowed (e.g., Wix ADI, Framer AI, ChatGPT). Assets must be free-to-use/AI-generated",
        "Judging Criteria: Visual appeal, creativity, relevance, functionality, clarity, and Q&A"
      ],
      "registerLink": "https://forms.gle/urDW64TRHMx2SdYe9"
    },
    {
      "id": "paper-presentation",
      "name": "Paper Presentation",
      "description": "Present cutting-edge research and technical papers on current technologies. This event provides a platform to share knowledge and insights on Computer Science, emerging technologies, and their real-world applications. The session will be held on Sept 3rd, 11:00 AM - 1:00 PM.",
      "guidelines": [
        "Team Size: Individual or 2 - 4 members",
        "Presentation: 7 mins + 2 mins Q&A",
        "Paper Guidelines: IEEE format; title clearly mentioned; topics may include AI, ML, Blockchain, IoT, Cybersecurity, etc.",
        "Submission: PPT & paper to be submitted by Sept 2nd via provided Drive link",
        "Judging Criteria: Content quality, presentation skills, and technical depth"
      ],
      "registerLink": "https://forms.gle/RsoNkSpyTnCobyvPA"
    },
    {
      "id": "project-expo",
      "name": "Project Expo",
      "description": "Showcase your innovative technical projects and prototypes on Sept 3rd, 11:00 AM - 3:00 PM. This is your chance to bring your ideas to life and present them to a panel of experts.",
      "guidelines": [
        "Team size: 3 - 4 members",
        "Bring a working prototype or fully deployed project with a poster/chart",
        "Poster/chart must include problem statement, methodology, and architecture diagram (you can add some more - optional)",
        "Presentation time: 5–7 minutes, followed by Q&A with judges",
        "Judging criteria: Creativity, innovation, technical execution, clarity, and impact"
      ],
      "registerLink": "https://forms.gle/c4isYskjKsAGmoKC9"
    },
    {
      "id": "technical-quiz",
      "name": "Tech Quiz",
      "description": "This is not just a quiz — it’s a battlefield of brains. Focused on latest technology trends, it will be held on Sept 4th, 9:30 AM - 11:00 AM. The clock is ticking, the questions are waiting, and only the top minds will rise to glory.",
      "guidelines": [
      "Team size: 2 - 4 members",
      "Quiz consists of 2 online rounds on the event website (Topics: C, Python, Quantum Technology, AI)",
      "Round 1: All teams compete → Top 15 advance to Round 2 (final battle for the crown)",
      "Winners: Top 3 teams ranked highest on the leaderboard",
      "AI restriction: No AI tools (ChatGPT, Gemini, etc.). After registration, only the team leader receives a unique 5-digit code to log in and access the quiz"
    ],

      "registerLink": "https://forms.gle/jXujsJzudet7WznG8"
    }
  ],
  "non_technical": [
    {
      "id": "mad-ad",
      "name": "Mad-Ad (Advertising Event)",
      "description": "Unleash your advertising creativity! Create compelling and humorous advertisements for given products or services. This event is designed to test your marketing skills, creativity, and ability to engage audiences. The event will be held on Sept 4th, 10:30 AM - 11:30 AM.",
      "guidelines": [
        "Team size: 2 - 3 members",
        "Topic/product can be chosen by your own",
        "Presentation time: 5–7 minutes, followed by Q&A with judges (Product review)",
        "Judged on creativity, humor, and audience engagement",
        "Props and Costumes allowed"
      ],
      "registerLink": "https://forms.gle/yqsijTrKNh1yA4eN6"
    },
    {
      "id": "esports",
      "name": "e-Sports",
      "description": "Get ready to showcase your gaming skills in the ultimate virtual battleground! From strategy to lightning-fast reflexes, E-Sports is where competitive spirit meets digital entertainment. Join us on September 4th, 2025, from 10:30 PM to 3 PM for thrilling multiplayer matches, to challenge top players, and to claim your place as the champion. Whether you're a casual gamer or a pro, this is your arena to shine!",
      "guidelines": [
      "Free Fire tournament (Br - like old free fire - no revival system)",
      "Bring your own mobile and headphones (No headphones = phone will be muted!)",
      "Top 3 Teams who complete all rounds will be declared winners",
      "Any loud noises, disturbances, cheating, or external assistance will get your team eliminated (Fair play policy)",
      "Tournament format will be announced on event day"
    ],

      "registerLink": "https://forms.gle/QfUBsF8N9pCyyRCY9"
    },
    {
      "id": "pot-painting",
      "name": "Pot/Painting Competition",
      "description": "Express your artistic talents through painting! Whether decorating pots or creating pictures, this competition gives you the chance to showcase your creativity and artistic skills in a colorful way. The event will be held on Sept 3rd, 2025, from 11:00 AM - 12:30 PM.",
      "guidelines": [
        "Individual participation",
        "Participants must choose between pot decoration or picture painting",
        "Pots and pictures will be provided, but painting materials (colors, brushes, etc.) must be brought by participants",
        "Theme will be announced on the spot, Time limit: 1 and 30 hours",
        "Judged on creativity, technique, and overall visual appeal"
      ],
      "registerLink": "https://forms.gle/d5GMeKGuvKTKbHeV7"
    },
    {
      "id": "photography",
      "name": "Photography Contest",
      "description": "Capture the soul of our campus with your lens in the Photography Contest on Sept 3rd, 12:00 PM - 1:00 PM. One shot, one chance - let your picture speak louder than words.",
      "guidelines": [
        "Team Size: 1; photos must be taken on campus during the event",
        "Devices: Bring your own camera/phone (no devices provided)",
        "Rules: No editing, filters, or AI use (AI = disqualification)",
        "Requirements: Each photo must have a creative tagline + 1 - 2 min explanation",
        "Judging Criteria: Composition, creativity, relevance, tagline, and explanation quality"
      ],
      "registerLink": "https://forms.gle/1rCRPjf4SeE4b1J18"
    },
    {
      "id": "non-tech-quiz",
      "name": "Non-Tech Quiz",
      "description": "Not everything is about coding and circuits - here your knowledge of the world takes the spotlight. Covering sports, movies, current affairs, and trivia, this quiz will be held on Sept 3rd, 3:00 PM - 4:00 PM.",
      "guidelines": [
      "Team size: 2 - 4 members",
      "Quiz consists of 2 online rounds on the event website (Topics: Current Affairs - India, Sports, General Knowledge)",
      "Round 1: All teams compete → Top 15 advance to Round 2 (final battle for the title)",
      "Winners: Top 3 teams ranked highest on the leaderboard",
      "AI restriction: No AI tools (ChatGPT, Gemini, etc.). After registration, only the team leader receives a unique 5-digit code to log in and access the quiz"
    ],

      "registerLink": "https://forms.gle/hE32XB2ZawP72sfG8"
    },
    {
      "id": "memes",
      "name": "Meme Creation Contest",
      "description": "Create viral - worthy memes that capture current trends and humor! This event is all about showcasing your understanding of internet culture and your ability to create relatable, funny, and creative content. The contest will be held on Sept 3rd, 11:30 AM - 12:40 PM.",
      "guidelines": [
        "Team size: 2 - 3 participants",
        "Create 3-4 original memes on given themes the theme will be announced at the start of the event",
        "Use any meme creation tools or software",
        "All content must be appropriate and non-offensive, Time limit: 20 - 30 minutes to build the memes",
        "Judged on humor, creativity, and relatability to current trends"
      ],
      "registerLink": "https://forms.gle/KvPUrvj8pVB5RJM29"
    }
  ]
};


// Global variables
let currentEvent = null;
let allEvents = [];

// Initialize the application when DOM is loaded
document.addEventListener('DOMContentLoaded', function() {
    console.log('🚀 ANTHARIKSHA 2K25 - Initializing application...');
    
    // Create combined events array for easy lookup
    allEvents = [...eventData.technical, ...eventData.non_technical];
    
    initSplashScreen();
    setupEventListeners();
    
    console.log('✅ Application initialized successfully!');
});

// Spectacular Rocket Launch Splash Screen
function initSplashScreen() {
    console.log('🎬 Initializing spectacular splash screen...');
    
    const splashScreen = document.getElementById('splashScreen');
    const mainSite = document.getElementById('mainSite');
    
    if (!splashScreen || !mainSite) {
        console.error('❌ Critical elements not found');
        return;
    }
    
    // Add dynamic particle effects
    addDynamicParticles();
    
    // Start the rocket launch sequence
    setTimeout(() => {
        console.log('🚀 Rocket launch sequence initiated!');
        
        // Add screen shake effect
        document.body.style.animation = 'screenShake 0.8s ease-in-out';
        
        setTimeout(() => {
            document.body.style.animation = '';
        }, 800);
        
    }, 1000);
    
    // Complete splash sequence and transition to main site
    setTimeout(() => {
        console.log('🌌 Transitioning to main website...');
        
        // Fade out splash screen
        splashScreen.style.transition = 'all 0.8s ease-in-out';
        splashScreen.style.opacity = '0';
        splashScreen.style.transform = 'scale(1.1)';
        
        // Show main site with smooth transition
        setTimeout(() => {
            splashScreen.style.display = 'none';
            mainSite.classList.remove('hidden');
            mainSite.classList.add('show');
            
            // Add entrance animation to main content and populate events
            animateMainContentEntrance();
            populateEvents();
            
            console.log('✨ Welcome to ANTHARIKSHA 2K25!');
        }, 800);
        
    }, 4500); // Total splash duration: 4.5 seconds
}

// Add dynamic particle effects for enhanced visual impact
function addDynamicParticles() {
    const particleContainer = document.querySelector('.particles');
    
    if (!particleContainer) return;
    
    // Add more particles for spectacular effect
    for (let i = 0; i < 15; i++) {
        const particle = document.createElement('div');
        particle.className = 'particle';
        particle.style.setProperty('--delay', `${Math.random() * 2}s`);
        particle.style.setProperty('--x', `${Math.random() * 100}%`);
        particle.style.setProperty('--y', `${70 + Math.random() * 20}%`);
        
        // Random colors for particles
        const colors = ['#FF6B35', '#FFD700', '#4A90E2', '#FF4444'];
        particle.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
        
        particleContainer.appendChild(particle);
    }
}

// Animate main content entrance
function animateMainContentEntrance() {
    const header = document.querySelector('.header');
    const sections = document.querySelectorAll('.events-section');
    
    if (header) {
        header.style.opacity = '0';
        header.style.transform = 'translateY(-30px)';
        header.style.transition = 'all 0.8s ease-out';
        
        setTimeout(() => {
            header.style.opacity = '1';
            header.style.transform = 'translateY(0)';
        }, 200);
    }
    
    sections.forEach((section, index) => {
        section.style.opacity = '0';
        section.style.transform = 'translateY(30px)';
        section.style.transition = 'all 0.8s ease-out';
        
        setTimeout(() => {
            section.style.opacity = '1';
            section.style.transform = 'translateY(0)';
        }, 400 + (index * 200));
    });
}

// Populate events in the grid with enhanced animations
function populateEvents() {
    console.log('🎯 Populating event cards...');
    const technicalEventsGrid = document.getElementById('technicalEvents');
    const nonTechnicalEventsGrid = document.getElementById('nonTechnicalEvents');
    
    if (!technicalEventsGrid || !nonTechnicalEventsGrid) {
        console.error('❌ Event grid elements not found');
        return;
    }
    
    // Clear existing content
    technicalEventsGrid.innerHTML = '';
    nonTechnicalEventsGrid.innerHTML = '';
    
    // Populate technical events with proper event binding
    eventData.technical.forEach((event, index) => {
        console.log(`📋 Creating technical event: ${event.name} (ID: ${event.id})`);
        const eventCard = createEventCard(event, 'technical', index);
        technicalEventsGrid.appendChild(eventCard);
    });

    // Populate non-technical events with proper event binding
    eventData.non_technical.forEach((event, index) => {
        console.log(`🎨 Creating non-technical event: ${event.name} (ID: ${event.id})`);
        const eventCard = createEventCard(event, 'non-technical', index);
        nonTechnicalEventsGrid.appendChild(eventCard);
    });
    
    console.log('✅ All event cards created successfully!');
}

// Create enhanced event card with proper event binding
function createEventCard(event, category, index) {
    const card = document.createElement('div');
    card.className = 'event-card';
    card.innerHTML = `<h3>${event.name}</h3>`;
    
    // Set data attributes for debugging
    card.setAttribute('data-event-id', event.id);
    card.setAttribute('data-category', category);
    card.setAttribute('data-event-name', event.name);
    
    // Store a reference to the original event data (not a copy to avoid issues)
    card.eventData = event;
    
    // Add staggered animation delay for entrance
    card.style.animationDelay = `${index * 0.1}s`;
    
    // Enhanced click event with proper event binding and debugging
    card.addEventListener('click', function(e) {
        e.preventDefault();
        e.stopPropagation();
        
        console.log(`🎯 Event card clicked: "${event.name}" (ID: ${event.id})`);
        console.log('Event data:', event);
        
        // Add click visual feedback
        this.style.transform = 'scale(0.95)';
        
        setTimeout(() => {
            this.style.transform = '';
            // Use the event data directly from closure
            openEventModal(event);
        }, 150);
    });

    // Enhanced hover effects
    card.addEventListener('mouseenter', function(e) {
        this.style.transform = 'translateY(-8px) scale(1.02)';
        this.style.boxShadow = '0 15px 40px rgba(255, 107, 53, 0.3), 0 5px 15px rgba(0, 0, 0, 0.2)';
    });
    
    card.addEventListener('mouseleave', function(e) {
        this.style.transform = '';
        this.style.boxShadow = '';
    });

    // Add ripple effect on click
    card.addEventListener('mousedown', function(e) {
        createRippleEffect(e, this);
    });

    // Ensure the card is interactive
    card.style.cursor = 'pointer';
    card.style.userSelect = 'none';

    console.log(`✅ Event card created for: ${event.name} (${category})`);
    return card;
}

// Create spectacular ripple effect
function createRippleEffect(event, element) {
    const ripple = document.createElement('span');
    const rect = element.getBoundingClientRect();
    const size = Math.max(rect.width, rect.height);
    const x = event.clientX - rect.left - size / 2;
    const y = event.clientY - rect.top - size / 2;
    
    ripple.style.cssText = `
        position: absolute;
        width: ${size}px;
        height: ${size}px;
        left: ${x}px;
        top: ${y}px;
        background: radial-gradient(circle, rgba(255, 107, 53, 0.4) 0%, transparent 70%);
        border-radius: 50%;
        transform: scale(0);
        animation: rippleExpand 0.6s linear;
        pointer-events: none;
        z-index: 10;
    `;
    
    element.style.position = 'relative';
    element.appendChild(ripple);
    
    setTimeout(() => {
        if (ripple.parentNode) {
            ripple.remove();
        }
    }, 600);
}

// Open event detail modal with enhanced animations and proper data handling
function openEventModal(event) {
    console.log(`🚀 Opening modal for event: "${event.name}" (ID: ${event.id})`);
    console.log('Modal event data:', event);
    
    if (!event || !event.name || !event.description) {
        console.error('❌ Invalid event data provided:', event);
        return;
    }
    
    // Store current event globally
    currentEvent = event;
    
    const eventModal = document.getElementById('eventModal');
    const modalEventName = document.getElementById('modalEventName');
    const modalEventDescription = document.getElementById('modalEventDescription');
    const modalEventGuidelines = document.getElementById('modalEventGuidelines');
    
    if (!eventModal || !modalEventName || !modalEventDescription || !modalEventGuidelines) {
        console.error('❌ Modal elements not found');
        return;
    }
    
    // Populate modal content with the correct event data
    modalEventName.textContent = event.name;
    modalEventDescription.textContent = event.description;
    
    // Clear and populate guidelines with enhanced styling
    modalEventGuidelines.innerHTML = '';
    
    if (event.guidelines && Array.isArray(event.guidelines) && event.guidelines.length > 0) {
        event.guidelines.forEach((guideline, index) => {
            const li = document.createElement('li');
            li.textContent = guideline;
            li.style.animationDelay = `${index * 0.1}s`;
            li.classList.add('guideline-item');
            modalEventGuidelines.appendChild(li);
        });
    } else {
        // Add fallback if no guidelines
        const li = document.createElement('li');
        li.textContent = 'Guidelines will be provided during event registration.';
        li.classList.add('guideline-item');
        modalEventGuidelines.appendChild(li);
    }

    // Update register button based on registration link availability
    updateRegisterButton(event.registerLink);

    // Show modal with spectacular entrance
    eventModal.classList.remove('hidden');
    eventModal.style.display = 'flex';
    eventModal.style.opacity = '1';
    eventModal.style.visibility = 'visible';
    
    // Prevent background scrolling
    document.body.style.overflow = 'hidden';
    
    // Add modal entrance animation
    const modalContent = eventModal.querySelector('.modal-content');
    if (modalContent) {
        modalContent.style.transform = 'scale(0.8) translateY(20px) rotateX(15deg)';
        modalContent.style.opacity = '0';
        
        requestAnimationFrame(() => {
            modalContent.style.transition = 'all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275)';
            modalContent.style.transform = 'scale(1) translateY(0) rotateX(0deg)';
            modalContent.style.opacity = '1';
        });
    }
    
    console.log(`✨ Modal successfully opened for: ${event.name}`);
}

// Update register button based on link availability
function updateRegisterButton(registerLink) {
    const registerBtn = document.getElementById('registerBtn');
    
    if (!registerBtn) {
        console.error('❌ Register button not found');
        return;
    }
    
    if (registerLink && registerLink.trim() !== '') {
        // Enable button for valid links
        registerBtn.textContent = 'Register Now';
        registerBtn.disabled = false;
        registerBtn.classList.remove('btn--disabled');
        registerBtn.classList.add('btn--primary');
        registerBtn.style.cursor = 'pointer';
        registerBtn.style.opacity = '1';
        
        console.log(`✅ Register button enabled with link: ${registerLink}`);
    } else {
        // Disable button when no link available
        registerBtn.textContent = 'Link Coming Soon';
        registerBtn.disabled = true;
        registerBtn.classList.add('btn--disabled');
        registerBtn.classList.remove('btn--primary');
        registerBtn.style.cursor = 'not-allowed';
        registerBtn.style.opacity = '0.6';
        
        console.log('⏳ Register button disabled - no link available');
    }
}

// Close modal with enhanced exit animation
function closeEventModal() {
    console.log('🚪 Closing event modal...');
    const eventModal = document.getElementById('eventModal');
    
    if (!eventModal) {
        console.error('❌ Modal element not found');
        return;
    }
    
    const modalContent = eventModal.querySelector('.modal-content');
    
    // Exit animation
    if (modalContent) {
        modalContent.style.transition = 'all 0.3s ease-in-out';
        modalContent.style.transform = 'scale(0.9) translateY(20px)';
        modalContent.style.opacity = '0';
    }
    
    setTimeout(() => {
        eventModal.classList.add('hidden');
        eventModal.style.display = 'none';
        eventModal.style.opacity = '0';
        eventModal.style.visibility = 'hidden';
        
        // Restore scrolling
        document.body.style.overflow = '';
        
        // Reset modal content
        if (modalContent) {
            modalContent.style.transform = '';
            modalContent.style.opacity = '';
            modalContent.style.transition = '';
        }
        
        currentEvent = null;
    }, 300);
    
    console.log('✅ Modal closed successfully');
}

// Enhanced register button functionality
function handleRegisterClick() {
    if (!currentEvent) {
        console.error('❌ No current event selected');
        return;
    }
    
    const registerLink = currentEvent.registerLink;
    const eventName = currentEvent.name;
    
    console.log(`📝 Register button clicked for: ${eventName}`);
    console.log(`Registration link: ${registerLink}`);
    
    if (registerLink && registerLink.trim() !== '') {
        // Open the registration link in a new tab
        try {
            const newWindow = window.open(registerLink, '_blank', 'noopener,noreferrer');
            if (newWindow) {
                console.log(`✅ Opened registration form in new tab: ${registerLink}`);
                
                // Provide user feedback
                const registerBtn = document.getElementById('registerBtn');
                if (registerBtn) {
                    const originalText = registerBtn.textContent;
                    registerBtn.textContent = 'Opening Form...';
                    registerBtn.style.background = 'linear-gradient(45deg, #4CAF50, #45a049)';
                    
                    setTimeout(() => {
                        registerBtn.textContent = originalText;
                        registerBtn.style.background = '';
                    }, 2000);
                }
            } else {
                console.warn('⚠️ Pop-up blocked - showing fallback message');
                alert(`Registration form link:\n${registerLink}\n\nPlease copy this link and open it in a new tab to register for ${eventName}.`);
            }
        } catch (error) {
            console.error('❌ Error opening registration link:', error);
            alert(`Please visit this link to register:\n${registerLink}`);
        }
    } else {
        console.log('⏳ Registration link not available yet');
        // Button should be disabled, but show message if somehow clicked
        alert(`Registration for "${eventName}" will be available soon!\n\nPlease check back later for the registration link.`);
    }
}

// Setup enhanced event listeners
function setupEventListeners() {
    console.log('🎛️ Setting up event listeners...');
    
    const closeModal = document.getElementById('closeModal');
    const eventModal = document.getElementById('eventModal');
    const registerBtn = document.getElementById('registerBtn');
    
    // Close modal button with enhanced functionality
    if (closeModal) {
        closeModal.addEventListener('click', function(e) {
            e.preventDefault();
            e.stopPropagation();
            console.log('❌ Close button clicked');
            closeEventModal();
        });
        
        // Add hover effect to close button
        closeModal.addEventListener('mouseenter', function() {
            this.style.transform = 'scale(1.1) rotate(90deg)';
            this.style.background = 'rgba(255, 107, 53, 0.2)';
        });
        
        closeModal.addEventListener('mouseleave', function() {
            this.style.transform = 'scale(1) rotate(0deg)';
            this.style.background = '';
        });
    }
    
    // Close modal on background click
    if (eventModal) {
        eventModal.addEventListener('click', function(e) {
            if (e.target === eventModal) {
                console.log('🖱️ Background clicked - closing modal');
                closeEventModal();
            }
        });
    }

    // Close modal with Escape key
    document.addEventListener('keydown', function(e) {
        if (e.key === 'Escape' && eventModal && !eventModal.classList.contains('hidden')) {
            console.log('⌨️ Escape key pressed - closing modal');
            closeEventModal();
        }
    });

    // Enhanced register button with new functionality
    if (registerBtn) {
        registerBtn.addEventListener('click', function(e) {
            e.preventDefault();
            e.stopPropagation();
            
            // Only proceed if button is not disabled
            if (!this.disabled) {
                // Add visual feedback
                this.style.transform = 'scale(0.95)';
                
                setTimeout(() => {
                    this.style.transform = 'scale(1)';
                    handleRegisterClick();
                }, 150);
            }
        });
        
        // Add enhanced hover effects to register button (only when not disabled)
        registerBtn.addEventListener('mouseenter', function() {
            if (!this.disabled) {
                this.style.boxShadow = '0 0 20px rgba(255, 107, 53, 0.6)';
                this.style.transform = 'translateY(-2px)';
            }
        });
        
        registerBtn.addEventListener('mouseleave', function() {
            if (!this.disabled) {
                this.style.boxShadow = '';
                this.style.transform = '';
            }
        });
    }

    // Add smooth scrolling
    document.documentElement.style.scrollBehavior = 'smooth';
    
    // Add performance optimization styles
    addPerformanceOptimizations();
    
    console.log('✅ All event listeners configured successfully!');
}

// Add performance optimizations and additional styles
function addPerformanceOptimizations() {
    const style = document.createElement('style');
    style.textContent = `
        @keyframes rippleExpand {
            to {
                transform: scale(4);
                opacity: 0;
            }
        }
        
        .guideline-item {
            opacity: 0;
            transform: translateX(-20px);
            animation: slideInLeft 0.5s ease-out forwards;
        }
        
        @keyframes slideInLeft {
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }
        
        /* Enhanced performance */
        .event-card {
            will-change: transform, box-shadow;
            backface-visibility: hidden;
        }
        
        .modal-content {
            will-change: transform, opacity;
            backface-visibility: hidden;
        }
        
        /* Ensure all event cards are clickable */
        .event-card {
            cursor: pointer !important;
            user-select: none;
            pointer-events: all;
        }
        
        .event-card:hover {
            border-color: var(--rocket-orange) !important;
        }
        
        /* Enhanced register button styling */
        #registerBtn {
            font-weight: 600;
            letter-spacing: 0.5px;
            text-transform: uppercase;
            transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }
        
        #registerBtn:active:not(:disabled) {
            transform: scale(0.98) translateY(1px);
        }
        
        /* Disabled button styling */
        .btn--disabled {
            background: rgba(98, 108, 113, 0.3) !important;
            color: rgba(255, 255, 255, 0.5) !important;
            border: 1px solid rgba(119, 124, 124, 0.3) !important;
            cursor: not-allowed !important;
            opacity: 0.6 !important;
        }
        
        .btn--disabled:hover {
            transform: none !important;
            box-shadow: none !important;
            background: rgba(98, 108, 113, 0.3) !important;
        }
        
        /* Reduced motion support */
        @media (prefers-reduced-motion: reduce) {
            .event-card,
            .modal-content,
            .guideline-item {
                animation: none !important;
                transition: none !important;
            }
        }
        
        /* Mobile optimization */
        @media (max-width: 768px) {
            .event-card {
                padding: 1rem;
                margin: 0.5rem 0;
            }
            
            .modal-content {
                margin: 1rem;
                max-height: 85vh;
            }
        }
    `;
    document.head.appendChild(style);
}

// Add CSS pulse animation
const pulseStyle = document.createElement('style');
pulseStyle.textContent = `
    @keyframes pulse {
        0% { transform: scale(1); }
        50% { transform: scale(1.05); }
        100% { transform: scale(1); }
    }
`;
document.head.appendChild(pulseStyle);

console.log('🌟 ANTHARIKSHA 2K25 - Application loaded and ready for initialization!');
